import styles from './Reset-password.module.css';
const ResetPassword = () => {
  return (
    <section className={styles.resetPassword}>
      <p>Страница сброса пароля</p>
    </section>
  );
};

export default ResetPassword;
